
const API_VERSION = 1;
const API_URL = `/api/v${API_VERSION}`;
const API_CONTENT_TYPE = 'application/json';


const commonConfig = {
    API_VERSION,
    API_URL,
    API_CONTENT_TYPE
}

export default commonConfig;